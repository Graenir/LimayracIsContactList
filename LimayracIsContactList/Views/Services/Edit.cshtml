@model LimayracIsContactList.ViewModels.ServiceViewModel
@{
    ViewData["Title"] = "Edit";
}

<div class="container-panel">
    <div class="container">

        <h1>Edit</h1>

        <h4>Service</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <form asp-action="Edit" id="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="Id" />
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Entreprise.Name" class="control-label"></label>
                        <select asp-for="Entreprise.Id" class="form-control" asp-items="Model.Options"></select>
                        <span asp-validation-for="Entreprise.Id" class="text-danger"></span>
                    </div>
                </form>
            </div>
        </div>

        <div class="footer-button">
            <input type="submit" value="Save" class="btn btn-success" form="Edit"/>
            <a asp-action="Index" class="btn btn-dark">Back to List</a>
        </div>

    </div>
</div>

